services:
  app:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped

  # Redis opcional para cache (descomente quando implementado)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   restart: unless-stopped

# volumes:
#   redis_data:
